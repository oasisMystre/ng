import { Context } from "../ng/core";
import { Button } from "../ng/widgets";

export class IconButton extends Button {
    build(context: Context): HTMLElement {
        const wrapper = super.build(context);
        
    }
}
export class StyledButton {}
export class RoundedButton {}
